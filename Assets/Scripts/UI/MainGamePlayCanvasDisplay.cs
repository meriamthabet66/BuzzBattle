using System;
using UnityEngine;

namespace UI {
    public class MainGamePlayCanvasDisplay : MonoBehaviour {
        private void Start() {
            gameObject.SetActive(false);   
        }
    }
}